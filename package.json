{
    "name": "cs261-typeshi",
    "version": "1.0.0",
    "description": "",
    "main": "server.js",
    "type": "module",
    "scripts": {
        "dev": "node server.js",
        "docker:build": "docker build -f DockerContainer_NodeJS.dockerfile -t node-js-image .",
        "docker:run": "docker run -d --name node-server --network mynetwork -p 3000:3000 node-js-image",
        "docker:stop": "docker stop node-server && docker rm node-server",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.20.0"
    }
}
